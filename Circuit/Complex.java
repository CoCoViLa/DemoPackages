import java.util.*;

class Complex {
    /*@ specification Complex {
	double re, im, arg, mod;
	mod^2 = re^2 + im^2;
	mod * cos(arg) = re;
	
    }@*/


	//double shift;
	//re, im -> shift{getShift};
	//arg = shift + atan(im/re);
	//tan(arg - shift) = im / re;
	//mod * sin(arg) = im;
//	public double getShift( double a, double b ) {
//		return a >= 0 ? 0 : Math.PI;
//	}
}

